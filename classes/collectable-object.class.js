/**
 * Class representing a collectible object in the game, which extends a movable object.
 */
class CollactableObject extends MovableObject {
  /**
   * Creates a new collectible object instance.
   */
  constructor(world) {
    super(world);
  }
}
