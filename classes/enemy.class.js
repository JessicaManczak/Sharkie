/**
 * Class representing an enemy in the game, extending the movable object.
 */
class Enemy extends MovableObject {
  /**
   * Creates a new enemy instance.
   */
  constructor(world) {
    super(world);
  }
}
